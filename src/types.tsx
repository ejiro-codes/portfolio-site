export type projects = {
  name: string;
  stack: string[];
  github: string;
  description: string;
  image?: string;
  video?: string;
};

export type experience = {
  jobTitle: string;
  yoe: number;
  description: string;
};
